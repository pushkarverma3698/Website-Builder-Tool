{
  "name": "cinematic-web",
  "private": true,
  "version": "0.0.0",
  "description": "The cinematic shadcn/ui — premium design systems as code, 6 presets, 3D mode, think-to-build pipeline.",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm --filter './packages/*' -r build",
    "dev": "pnpm --filter './packages/*' -r --parallel dev",
    "lint": "pnpm --filter './packages/*' -r lint",
    "type-check": "pnpm --filter './packages/*' -r type-check",
    "test": "pnpm --filter './packages/*' -r test",
    "sync:claude": "tsx scripts/sync-claude.ts",
    "clean": "pnpm -r --parallel exec rm -rf dist node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
